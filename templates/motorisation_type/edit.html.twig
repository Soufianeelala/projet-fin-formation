{% extends 'base.html.twig' %}

{% block title %}Edit MotorisationType{% endblock %}

{% block main %}
    <h1>Edit MotorisationType</h1>

    {% if is_granted('ROLE_ADMIN') %}
        {{ include('motorisation_type/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('motorisation_type/_delete_form.html.twig') }}
    {% else %}
        <p>⚠️ Vous n'avez pas l'autorisation de modifier ce type de motorisation.</p>
    {% endif %}

    <a href="{{ path('app_motorisation_type_index') }}">Retour à la liste</a>
{% endblock %}
